<!--index.wxml-->
<view class="container">
    <button  class="sim-btn sim-btn-info" wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 请授权获取头像和昵称 </button>
</view>